# Minimal .env Configuration
# Copy this as a starting point for new deployments
# Only these settings need to be explicitly set; all others have sensible defaults

# ============================================================================
# CRITICAL (Must be set for production)
# ============================================================================

# Secret key for signing JWT tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY="change-me-to-a-secure-random-string-minimum-32-chars"

# ============================================================================
# OPTIONAL - Email Configuration (for sending emails)
# ============================================================================

# Choose email provider: dev, gmail, sendgrid, custom, curtin
# - dev: prints emails to console (safe for local dev)
# - gmail: uses Gmail SMTP with app-specific password
# - sendgrid: uses SendGrid API
# - custom: generic SMTP server
# - curtin: Curtin University's SMTP server
EMAIL_PROVIDER="dev"

# If using EMAIL_PROVIDER="gmail":
# GMAIL_APP_PASSWORD="your-16-char-app-password"

# If using EMAIL_PROVIDER="sendgrid":
# SENDGRID_API_KEY="SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# If using EMAIL_PROVIDER="custom":
# SMTP_SERVER="mail.example.com"
# SMTP_PORT=587
# SMTP_USER="noreply@example.com"
# SMTP_PASSWORD="your-smtp-password"

# ============================================================================
# OPTIONAL - Authentication
# ============================================================================

# Email domain whitelist (comma-separated)
# Users can register if their email domain matches ANY of these
# Default: "curtin.edu.au"
# ALLOWED_DOMAINS="curtin.edu.au,borck.me"

# Specific email addresses allowed (comma-separated, optional)
# Users can register if their email EXACTLY matches ANY of these
# Useful for external collaborators, partners, or guests
# Default: empty (no specific emails needed)
# EMAIL_WHITELIST="partner@external.org,guest@company.com"

# ============================================================================
# OPTIONAL - Database
# ============================================================================

# Database URL
# Default: SQLite at ./ai_exchange.db (relative to backend directory)
# For PostgreSQL: postgresql://user:password@localhost:5432/ai_exchange
# DATABASE_URL="postgresql://user:password@localhost:5432/ai_exchange"

# ============================================================================
# OPTIONAL - Advanced Configuration
# ============================================================================

# Testing mode: disables rate limiting, uses mocked services
# DEBUG=true
# TESTING=true

# CORS origins (comma-separated, only for production)
# Default: http://localhost:5173,http://localhost:3000
# ALLOWED_ORIGINS="https://yourdomain.com,https://app.yourdomain.com"

# Rate limiting (format: "N/period" where period is second, minute, hour, day)
# RATE_LIMIT_LOGIN="5/minute"
# RATE_LIMIT_REGISTER="3/minute"
# RATE_LIMIT_FORGOT_PASSWORD="3/minute"
# RATE_LIMIT_RESET_PASSWORD="5/minute"
# RATE_LIMIT_READ="60/minute"
# RATE_LIMIT_WRITE="30/minute"

# ============================================================================
# QUICK START
# ============================================================================
#
# Development (local):
#   1. Copy this file to .env
#   2. Keep EMAIL_PROVIDER="dev" (prints to console)
#   3. Keep other defaults
#   4. Start: uvicorn app.main:app --reload
#
# Production (first deployment):
#   1. Copy this file to .env
#   2. Generate SECRET_KEY: python -c "import secrets; print(secrets.token_urlsafe(32))"
#   3. Set EMAIL_PROVIDER (gmail, sendgrid, custom, or curtin)
#   4. Set corresponding email credentials
#   5. Set ALLOWED_ORIGINS to your domain
#   6. Optional: Set ALLOWED_DOMAINS for multi-campus, EMAIL_WHITELIST for partners
#   7. Deploy with: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
#
