# The AI Exchange - Docker Environment Configuration
# Copy this file to backend/.env and customize for your deployment
# DO NOT commit the actual .env file to version control

# ============================================
# SECURITY (CRITICAL - CHANGE THESE!)
# ============================================
# Generate a strong secret key:
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-min-32-characters-change-in-production

# JWT configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================
# DATABASE
# ============================================
# SQLite database URL (persisted in ./data/db/)
DATABASE_URL=sqlite:///./data/db/ai_exchange.db

# ============================================
# API CONFIGURATION
# ============================================
PROJECT_NAME=The AI Exchange
API_V1_STR=/api/v1
DEBUG=false

# ============================================
# DOMAIN & ORIGIN CONFIGURATION
# ============================================
# Allowed origins for CORS (comma-separated)
# Include your VPS domain when deployed
# Format: https://yourdomain.com or http://localhost:8000 for local
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:5173

# Allowed email domains (users must have emails from these domains)
# Comma-separated list
ALLOWED_DOMAINS=curtin.edu.au

# Email whitelist (specific emails allowed, overrides domain whitelist)
# Comma-separated list of email addresses
EMAIL_WHITELIST=admin@curtin.edu.au

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=INFO
# Set to true for development/debugging only
DEBUG=false

# ============================================
# EMAIL/SMTP CONFIGURATION (Optional)
# ============================================
# Only needed if you want to send verification emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@curtin.edu.au
SMTP_FROM_NAME=The AI Exchange
SMTP_TLS=true

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 1. Copy this file to backend/.env
# 2. Update SECRET_KEY with a strong random value
# 3. Update ALLOWED_DOMAINS with your institution's domain
# 4. Update ALLOWED_ORIGINS with your VPS domain when deployed
# 5. If using email, configure SMTP settings
# 6. Never commit the .env file to git

# Example for VPS deployment:
# ALLOWED_ORIGINS=https://ai-exchange.yourdomain.com
# Caddy/Nginx will handle the HTTPS reverse proxy on yourdomain.com
# and forward requests to localhost:8000 where Docker container runs
